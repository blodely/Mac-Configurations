# Created By Luo Yu(indie.luo@gmail.com)
# Date: August 5, 2019

abbr -a gitpush 'git push && git gc'
abbr -a gitpull 'git pull && git gc'
abbr -a gitpullr 'git pull --rebase'
abbr -a openws 'open *.xcworkspace'
abbr -a tunnel 'sshuttle'

